# SLAAC-Task
🔹 Task Overview: The objective was to configure a router to support IPv6 and allow connected hosts to automatically generate their own IPv6 addresses without using a DHCPv6 server.  🔹 Topology Description:  One router connecting two LANs  Each LAN contains a switch and a PC  The router interfaces act as IPv6 gateways for both networks
